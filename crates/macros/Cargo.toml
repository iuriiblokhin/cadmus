[package]
name = "cadmus-macros"
version = "0.12.0"
edition = "2024"
authors = ["Cadmus Authors"]
description = "Proc macros for Cadmus (lease attribute, etc.)"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "3", features = ["full"] }
